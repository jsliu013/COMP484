Ian A.H. Postel
Joshua Liu

All components of the project should be functional. 
Signup and login both tested positive.
Database table creates successfully.
PHP is perfectly happy to speak to database, so long as the .sql file (or the dump) has been imported by the database first.
HTML, CSS simple but present.
JS denies mismatched password entries.
dbdump.txt contains dump of the database after testing it.
